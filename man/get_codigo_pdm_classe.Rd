% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/codigo-pdm-classe.R
\name{get_codigo_pdm_classe}
\alias{get_codigo_pdm_classe}
\title{Recuperar PDMs por código de PDM ou de classe}
\usage{
get_codigo_pdm_classe(codigo_pdm_classe, busca_classe = NULL, busca_pdm = NULL)
}
\arguments{
\item{codigo_pdm_classe}{Número inteiro positivo correspondente a um código
de PDM ou de classe.}

\item{busca_classe}{\code{NULL}, \code{TRUE} ou \code{FALSE}. Quando diferente de \code{NULL}, é
enviado como o parâmetro \code{buscaClasse}.}

\item{busca_pdm}{\code{NULL}, \code{TRUE} ou \code{FALSE}. Quando diferente de \code{NULL}, é
enviado como o parâmetro \code{buscaPdm}.}
}
\value{
Um \code{tibble} com todos os registros e campos retornados pela API. Uma
consulta válida sem resultados retorna zero linhas e as mesmas 12 colunas.
}
\description{
Consulta o endpoint \code{codigoPdmClasse} do catálogo de Materiais do CNBS. O
retorno segue o esquema público \code{ElasticPdm}, com nomes, ordem e tipos
estáveis.
}
\examples{
\dontrun{
get_codigo_pdm_classe(6505)
get_codigo_pdm_classe(6505, busca_classe = TRUE)
}

}
